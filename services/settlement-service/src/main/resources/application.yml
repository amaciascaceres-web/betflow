server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: settlement-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/betflow?currentSchema=settlement
    username: ${POSTGRES_USER:betflow}
    password: ${POSTGRES_PASSWORD:betflow}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: settlement

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
